날카로운 지적입니다. 부분적으로 맞습니다 — 지금 표에 채워진 값만 보면 두 열이 완전히 붙어 있고(모든 baseline ××, PACE만 ✓✓), 논리적으로도 한쪽이 다른 쪽을 함축합니다. 정리하면:

왜 같아 보이나
논리적 포함관계: "frame이 남은 창에 맞나(frame-fit)"라는 판단은 유한 창이 있어야만 의미가 있습니다. 무한 horizon이면 항상 맞으니까요. 즉 frame-fit ✓ ⟹ finite-window ✓ (역은 성립 안 함). → 한 열이 다른 열의 부분집합.
표의 값이 collinear: 현재 모든 행이 ×× 아니면 ✓✓라, 정보량 관점에서 두 열이 서로 구별을 못 해줌.
근본 원인은 한 열은 "환경(setting)"(finite-window), 다른 열은 그 환경을 전제로 한 "메커니즘"(frame-fit)이라 중첩(nested) 된다는 점입니다.

해결 방향 — 두 열을 "서로 독립인 능력(capability)"으로 재정의
표를 논문의 두 가지 요구사항(Intro에 이미 명시)에 맞추면 중첩이 풀립니다:

열	무엇을 측정	성격
Time-varying target tracking (req. i)	창이 소진되며 상승하는 τ*(t)를 추적하나 (정적 equilibrium 수렴이 아니라)	attempt-rate 동역학
Frame-fit self-exclusion (req. ii)	프레임 길이로 완료 불가 STA를 경쟁에서 배제하나	per-STA 프레임 실현성
Signaling-free	명시적 조정 불필요	조정 비용
이 둘은 직교합니다 (2×2로 확인):

A만(tracking, no fit): 동질 프레임 환경에서 τ*=1/remaining 추적하지만 self-exclusion 없음 → 가능
B만(fit, no tracking): 고정 τ + "안 맞으면 빠짐" fit-check → 가능
즉 한쪽이 다른 쪽을 함축하지 않음 → "같은 의미"라는 비판이 사라짐
중요한 현실 인정
재정의해도 이 표에선 여전히 baseline 전부 ××, PACE만 ✓✓ 입니다 (AND·PND·Cali는 정적 수렴이라 tracking ×, 전부 frame 무시라 fit ×). 값 패턴은 같아도 이제 두 열이 "서로 다른 두 기여"를 뜻하므로 중복이 아니라 "PACE가 두 독립 요건을 동시에 충족"으로 읽힙니다. 이건 정당한 comparison table 관례입니다.

반면 지금처럼 "finite-window(setting)" + "frame-fit(mechanism)" 조합은 중첩이라 "결국 같은 말"로 읽히는 게 맞습니다.

권장
window 열: NPCA target (finite-window) → Time-varying target tracking (또는 Window-adaptive rate) 로 능력 서술로 전환
frame 열: PPDU-aware → Frame-fit self-exclusion (또는 Viability-aware)
"NPCA" 단어는 캡션에서 유지 ("both required in the finite NPCA window") — 헤더 중복 부담 없이
이렇게 하면 앞서 [13] 코멘트(“NPCA target 뜻 모호”)와 이번 “두 열 동일” 우려가 동시에 해소됩니다.

한 가지 확인: window 열을 Time-varying target tracking(능력) 으로 바꾸는 데 동의하시나요? (지난번엔 NPCA target (finite-window)로 정하셨는데, 이 재정의가 바로 그 collinear 문제를 푸는 핵심입니다.) 동의하시면 frame 열 표현(Frame-fit self-exclusion vs Viability-aware)만 골라주시면 헤더·캡션·Phase (b) 정합까지 한 번에 반영하겠습니다.